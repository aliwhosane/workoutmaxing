# expo-router pulls a react-dom that floats ahead of the react version Expo
# pins, which npm 10 treats as an unresolvable peer. Expo owns these versions
# via `npx expo install`, so defer to its tree rather than npm strict mode.
legacy-peer-deps=true
